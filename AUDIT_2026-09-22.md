# 투리니 요청사항 전수 검토 보고서

검토일: 2026-09-22
검토 기준: GitHub `main` 최신본, 학습 데이터 864문항, 자동 테스트, 운영 빌드, 화면 흐름

## 판정 요약

| 요청사항 | 기존 상태 | 이번 반영·판정 |
| --- | --- | --- |
| 포트폴리오 기간 5년 → 10년 | `5년 이상`까지만 있음 | `5~10년`, `10년 이상`으로 확장하고 기본값을 `10년 이상`으로 변경 |
| 포트폴리오 항목·스케일·단위 | 내부 수치의 의미가 화면에서 불명확 | 위험도 `5~65 내부척도`, 종합 `0~85`, 나머지 `%`·`점` 단위를 명시 |
| 태그는 parent tag 20개만 표시 | 상세 태그가 저장될 수 있었음 | 진단·오답·기존 저장값을 상위 태그로 정규화하고 최대 20개만 표시 |
| 새 규칙엔진 근거 확인 | 저장소에는 v9.1만 존재 | v10 필수 시장자료가 없어 **미구현 판정**. 현재 엔진을 v10처럼 표시하지 않도록 안내 추가 |
| 강점 LLM·검수 CSV | 기존 A/B/C 점수 기준을 LLM이 재서술 | v10 초안과 충돌하여 이번 코드에는 새 강점 규칙을 임의 추가하지 않음. 아래 제안안으로 정책 확정 후 CSV 재생성 필요 |
| 720문항 업데이트 페이지 | GitHub에 해당 페이지 없음 | 저장소 문서의 720·180 표기를 864·216으로 수정. 외부 페이지는 링크/본문 필요 |
| 기능하지 않는 버튼 | 홈 공지 모양 요소가 버튼이지만 동작 없음 | 비기능 버튼 제거. 주요 버튼의 핸들러·잠금 조건 재검토 |
| 취약 태그 위치 | 결과 화면에만 일시 노출 | 홈·마이 화면에 상시 노출하고 태그를 누르면 해당 태그 우선 학습 |
| 한 학습 내 4개 유형 횟수 | 첫 노출이 4지선다에 치우칠 수 있음 | 10문제에서 네 유형이 각각 2~3회 나오도록 스케줄러 수정 |
| 해설 말투 | `~이다` 등 평서형 혼재 | 화면 출력 시 `~입니다/~습니다`로 정규화, 864문항 대표 어미 자동 검사 추가 |
| 진단 뒤 10문제 더 풀기 | 진단 결과에도 노출 | 진단 결과에서는 제거하고 홈 이동만 제공 |
| 약점태그 추천 반영 | 저장만 되고 출제 우선순위에 미반영 | 상위 태그와 상세 태그 모두 추천 점수에 연결 |
| 홈 큰 배너의 학습 종류 | 랜덤 학습처럼 보임 | `오늘의 맞춤 추천`으로 명시하고 취약 태그 우선 추천 문구 추가 |
| 왼쪽 상단 카테고리·Lv | 모든 화면에서 학습 문맥처럼 표시 | 학습 화면에서만 실제 카테고리·레벨, 나머지는 현재 메뉴명 표시 |
| 학습 탭 난이도 우회 | 잠긴 중급·고급을 직접 누를 수 있음 | 카테고리 목록의 직접 난이도 버튼 제거, 난이도 선택 화면의 잠금 규칙만 사용 |
| 진단 문제가 푼 문제 수에 포함 | 진단 문항 ID도 완료 목록에 저장 | 진단 문항을 `completedIds`와 학습 시도 수에서 제외 |
| 캐릭터 이미지 | 실제 경로와 코드 경로 불일치 | `/assets/...` 실제 공개 경로로 수정 |
| 환경설정 예시 | `.env.example` 없음 | 안전한 자리표시자만 포함한 예시 파일 추가 |

## 포트폴리오 v10 근거 검토

현재 저장소의 실제 구현은 `3.0.4-portfolio-v9.1`입니다. 고정 위험가중치, A/B/C/D 항목과 85점 종합점수를 사용합니다. 반면 검토 대상 v10 초안은 다음을 요구합니다.

- 최근 5년·주간·KRW 기준의 5개 시장 벤치마크 수익률
- 결측·주차 정렬·환율·배당 처리 규칙과 재현 가능한 전처리
- 공분산으로 계산한 연환산 변동성과 1~7 위험등급
- 벤치마크 제공자, 데이터 라이선스와 스냅샷 버전
- `benchmark_manifest.json`, `market_weekly_v1.csv`, `market_model_v1.json` 등 검증 산출물

이 파일과 확정 값은 GitHub에 없습니다. 따라서 고정가중치 v9.1을 v10 또는 ESMA 공식 등급처럼 바꾸는 것은 근거가 부족합니다. 이번 수정은 다음 안전조치까지만 적용했습니다.

1. 현재 화면이 v9.1 내부 상대척도임을 명시합니다.
2. 위험·적합도·분산도·집중 페널티의 단위를 분리해 표시합니다.
3. v10은 데이터·라이선스 확정과 별도 검증 뒤 전환하도록 안내합니다.

## 강점 출력 제안

v10 초안에는 `strengths` 필드를 쓰지 않는다고 되어 있어, 새 강점 규칙은 먼저 정책 승인이 필요합니다. 다시 도입한다면 LLM이 강점을 새로 판단하지 않고 규칙엔진이 만든 `verified_positive_factors`만 자연어로 옮기는 방식을 권장합니다.

| 우선순위 | 검증 가능한 긍정 요인 | 허용 표현 |
| ---: | --- | --- |
| 1 | 계산된 위험등급이 성향 허용 상한을 넘지 않음 | “성향 허용 범위 안입니다” |
| 2 | 투자기간별 채권+현금 최소기준 충족 | “기간 기준의 방어자산 조건을 충족합니다” |
| 3 | 개별주식 비중이 성향별 목표 상한 이내 | “개별주식 집중 기준을 넘지 않습니다” |
| 4 | 금 비중이 성향별 목표 상한 이내 | “금 비중 기준을 넘지 않습니다” |

강점이 없으면 빈 배열을 허용하고, 최대 3개만 출력하며, “최적·안전·적합 보장” 같은 표현은 금지해야 합니다. 사람 검수 CSV에는 계산 입력, 규칙 ID, 원문 사실, 표시 문장, 검수 결과를 함께 저장하는 것이 재현성에 유리합니다.

## 문항·태그 검증 결과

- 학습 문항: 864개
- 기본 개념: 216개, 개념별 4유형
- 상세 약점 태그: 204개
- 상위 태그: 정확히 20개
- 완전 중복·정규화 중복·선택지만 다른 중복: 0건
- 상위 태그 매핑 누락·오탈자·빈 값: 0건
- 2,500개 가상 세션에서 의도하지 않은 개념 중복: 0건
- 10문제 세션의 유형별 출제 수: 각 2~3개

## 검증 명령

```bash
npm test
npm run lint
npm run build
node scripts/audit-questions.mjs
```

최종 판정은 위 명령이 모두 통과하는 커밋을 기준으로 합니다.

최종 실행 결과: 자동 테스트 137개 통과, ESLint 경고·오류 0개, Next.js 운영 빌드 통과.
